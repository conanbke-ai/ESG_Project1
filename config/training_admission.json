{
  "contract": "solar-training-admission-policy.v2",
  "version": "2026-09-17-frozen-relational-1",
  "status": "frozen",
  "fixed_start_year": null,
  "selection_basis": [
    "quality_train_eligible",
    "generation_weather_overlap",
    "split_presence",
    "split_hourly_continuity",
    "non_train_split_rows_lte_train",
    "purge_and_sequence_history_loss"
  ],
  "recovered_historical_reference": {
    "source_commit": "2441c7fb51ec2bb1410b1b2f090f32928cbad27e",
    "actual_preprocessing_split": {
      "mode": "calendar",
      "train": "timestamp <= 2021-12-31T23:59:59",
      "validation": "2022-01-01T00:00:00 <= timestamp <= 2023-12-31T23:59:59",
      "test": "timestamp >= 2024-01-01T00:00:00",
      "purge_hours": 24
    },
    "plant_selection_rules": {
      "require_presence_in_train_validation_test": true,
      "reject_any_split_with_timestamp_gap_gt_hours": 1,
      "require_validation_rows_lte_train_rows": true,
      "require_test_rows_lte_train_rows": true
    },
    "explicit_fixed_minimum_rows_per_split": null,
    "minimum_rows_evidence_status": "NOT_FOUND_IN_VERSIONED_PREPROCESSING_CODE_PPT_OR_README",
    "excluded_bad_plants_evidence": "Historical excluded_bad_plants.csv stores train/val/test counts for 12 plants rejected because validation or test rows exceeded train rows."
  },
  "current_experiment_reference": {
    "source": "config/experiments/optimized.json",
    "mode": "fraction",
    "train_fraction": 0.6,
    "validation_fraction": 0.15,
    "calibration_fraction": 0.1,
    "test_fraction": 0.15,
    "purge_gap_hours": 168,
    "minimum_common_coverage": 0.95
  },
  "current_4way_extension": {
    "require_all_splits_present": true,
    "reject_split_timestamp_gap_gt_hours": 1,
    "require_validation_rows_lte_train_rows": true,
    "require_calibration_rows_lte_train_rows": true,
    "require_test_rows_lte_train_rows": true,
    "fixed_minimum_rows_per_split": null,
    "rationale": "Calibration is a current selection partition, so the recovered non-Train<=Train rule is applied consistently to Validation, Calibration, and Test without inventing a new minimum-N threshold."
  },
  "decision_semantics": {
    "ADMITTED": "Passes the frozen recovered relational rules in eligibility.v2. Model-specific usable sample counts are checked later by forecast readiness.",
    "REJECT": "Fails one or more frozen structural/relational rules. The source/service population remains preserved.",
    "HOLD": "Reserved for future policy states requiring explicit human review; frozen relational v2 should normally emit no HOLD decisions."
  },
  "population_separation": {
    "service_inventory_filtered_by_this_policy": false,
    "model_population_filtered_by_this_policy": true,
    "documentation": "docs/DATA_POPULATION_AND_MODEL_ELIGIBILITY.md"
  },
  "notes": [
    "No fixed minimum N per split is introduced because no versioned evidence for such a historical rule was found.",
    "The admission policy filters only the AI model population. EPSIS nationwide service inventory is not filtered by model eligibility.",
    "After this population gate, forecast_readiness remains responsible for model-specific lookback/horizon and effective sample losses.",
    "The current 60/15/10/15 split is not presented as the historical calendar split; only the plant-selection principle is recovered and extended."
  ]
}
